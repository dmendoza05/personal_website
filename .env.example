# Neon Postgres connection string (pooled recommended for serverless)
DATABASE_URL=

# Cloudflare Analytics API (server-side only — never expose to the browser)
# Production: wrangler secret put CF_ANALYTICS_TOKEN && wrangler secret put CF_ZONE_ID
# Local: set here or in .dev.vars
CF_ANALYTICS_TOKEN=
CF_ZONE_ID=
